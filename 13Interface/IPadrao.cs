interface IPadrao
{
// Sera criado 2 métodos onde a class calculo obrigatóriamente precisa ter.
// Não precisamos definir tipo "abstract" ou "public, private, protected" pois já é definodo por padrão "public".
void somar(int n1, int n2);

void subtracao( int n1, int n2);

}